name: Code Coverage

on:
  push:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  coverage:
    uses: OpenVoiceOS/gh-automations/.github/workflows/coverage.yml@dev
    secrets: inherit
    with:
      python_version: '3.12'
      coverage_source: 'ovos_skill_music_assistant'
      test_path: 'test/'
      test_extras: 'test'
      min_coverage: 0
